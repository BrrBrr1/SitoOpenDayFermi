// CONFIGURAZIONE FACILE DA MODIFICARE
const config = {
    // Modifica questi messaggi per personalizzare il sito
    welcomeMessages: [
        "Benvenuto nel futuro!",
        "Inizia il tuo viaggio nel digitale",
        "Codifica i tuoi sogni",
        "Trasforma le idee in realtà"
    ],

    // Modifica il numero di particelle (default: 50)
    particleCount: 50,

    // Modifica la velocità dell'animazione (millisecondi, default: 3000)
    animationSpeed: 3000,

    // Modifica i colori delle particelle (default: ['#6366f1', '#8b5cf6', '#ec4899', '#10b981'])
    particleColors: ['#6366f1', '#8b5cf6', '#ec4899', '#10b981']
};
